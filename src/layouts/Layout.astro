<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>Kershan Arulneswaran</title>
        <!-- Google Fonts: Instrument Serif and Stick No Bills -->
        <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif&family=Stick+No+Bills:wght@400;700&display=swap" rel="stylesheet">
    </head>
    <body>
        <slot />
    </body>
</html>

<style is:global>
html {
    scroll-behavior: auto;
}

html,
body {
    margin: 0;
    padding: 0;
    width: 100%;
    min-height: 100vh;
    font-family: 'Instrument Serif', 'Stick No Bills', serif, sans-serif;
    background-color: #f5f1e8;
    position: relative;
    overflow-x: hidden;
    overscroll-behavior: none;
}

body {
    position: relative;
}

.section {
    position: relative;
    z-index: 1;
    overflow: visible;
}

.bg-shape {
    content: '';
    position: absolute;
    z-index: -1;
    pointer-events: none;
}

/* Make header transparent with no background */
header {
    position: relative;
    z-index: 100;
    background: transparent !important;
    padding-top: 0;
    margin-top: 0;
}

footer {
    position: relative;
    z-index: 100;
    background: transparent !important;
}

/* Ensure content stays above blobs */
main,
section > *:not(.bg-shape) {
    position: relative;
    z-index: 1;
}

/* iMac G3 Colors */
.bondi-blue { background: #0095B6; }
.strawberry { background: #D84C6F; }
.lime { background: #7FBF4D; }
.tangerine { background: #F08A24; }
.grape { background: #6B4E8A; }

@keyframes float {
    0%, 100% {
        transform: translate(0, 0) scale(1) rotate(0deg);
    }
    33% {
        transform: translate(30px, -30px) scale(1.05) rotate(5deg);
    }
    66% {
        transform: translate(-20px, 20px) scale(0.95) rotate(-5deg);
    }
}

@keyframes pulse {
    0%, 100% {
        transform: scale(1) rotate(0deg);
    }
    50% {
        transform: scale(1.1) rotate(10deg);
    }
}

@keyframes morph {
    0%, 100% {
        border-radius: 63% 37% 54% 46% / 55% 48% 52% 45%;
    }
    25% {
        border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%;
    }
    50% {
        border-radius: 54% 46% 38% 62% / 49% 70% 30% 51%;
    }
    75% {
        border-radius: 70% 30% 50% 50% / 30% 65% 35% 70%;
    }
}

/* GPU acceleration for animations */
.bg-shape {
    transform: translateZ(0);
    backface-visibility: hidden;
}
</style>