---
import BlogPostLayout from './BlogPostLayout.astro';

interface Props {
    title: string;
    description: string;
    published: string;
    readingTime: string;
    heroImage: string;
    heroAlt: string;
    topics?: string[];
}
---

<BlogPostLayout {...Astro.props} variant="koops">
    <slot />
</BlogPostLayout>
